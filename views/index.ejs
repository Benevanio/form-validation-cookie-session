<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulário</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h2 class="mb-4 text-center">Formulário</h2>
        <form action="/form" method="POST" class="card p-4 shadow">
            <div class="mb-3">
                <label for="name" class="form-label">Nome:</label>
                <input type="text" class="form-control <%= nameError ? 'is-invalid' : '' %>" name="name" id="name" value="<%= nameValue || '' %>">
                <% if (nameError) { %> <div class="invalid-feedback"><%= nameError %></div> <% } %>
            </div>

            <div class="mb-3">
                <label for="email" class="form-label">E-mail:</label>
                <input type="email" class="form-control <%= emailError ? 'is-invalid' : '' %>" name="email" id="email" value="<%= emailValue || '' %>">
                <% if (emailError) { %> <div class="invalid-feedback"><%= emailError %></div> <% } %>
            </div>

            <div class="mb-3">
                <label for="pontos" class="form-label">Pontos:</label>
                <input type="number" class="form-control <%= pontosError ? 'is-invalid' : '' %>" name="pontos" id="pontos" value="<%= pontosValue || '' %>">
                <% if (pontosError) { %> <div class="invalid-feedback"><%= pontosError %></div> <% } %>
            </div>

            <button type="submit" class="btn btn-primary">Enviar</button>
        </form>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
